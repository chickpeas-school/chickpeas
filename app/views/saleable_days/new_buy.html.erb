<h3>Buy Day</h3>

<%= form_for @day, url: days_buy_path, local: true do |form| %>
  <% if @day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@day.errors.count, "error") %> prohibited this day from being saved:</h2>

      <ul>
      <% @day.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
	<% end %>

	<div class="form-group field">
		<%= form.label :date %>
		<%= datepicker_input form, :date %>
	</div>

	<%= form.hidden_field :buyer_id, value: @child.id %>

	<%= form.submit "Buy Day", class: "btn btn-primary" %>
	<%= link_to "Cancel", :back, class: "btn btn-secondary" %>
<% end %>
